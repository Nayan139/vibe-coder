/**
 * E2B default templates run as UID 1000 `user` with home `/home/user`.
 * Root-only paths like `/app` are not writable — clone and run here instead.
 */
export const E2B_PREVIEW_PROJECT_ROOT = "/home/user/vibe-preview";
